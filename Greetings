using UnityEngine;
using System.Collections;

public class Greetings : MonoBehaviour {
	public string myName;
	// Use this for initialization
	void Start () {
		SayHello (myName);
		SayGoodbye ();
	}
	public void SayHello (string name)		//The big point of this assignment is that the variable 'myName' is
	{						//supplied by the user and the function 'Main' takes it and uses
		print ("Hello " + myName);		//it to fill in the variable 'name' when it calls 'SayHello'.
	}						//You had it all right and then used 'Hello + myName' which shortcutted
	public void SayGoodbye ()			//passing myName to name. It should be 'print ("Hello " + name);'
	{						//Also, you didn't need to include the variable in 'SayGoodbye'.
		print ("Goodbye " + myName);
	}
	// Update is called once per frame
	void Update () {
	
	}
}
